<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offers for <%= item.title %></title>
</head>
<body>
    <h1>Offers for <%= item.title %></h1>
    <ul>
        <% offers.forEach(offer => { %>
            <li>
                <%= offer.user.firstName %> <%= offer.user.lastName %> - $<%= offer.amount.toFixed(2) %>
                <form action="/offers/<%= itemId %>/offers/<%= offer._id %>/accept" method="POST">
                    <button type="submit">Accept</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <a href="/items/<%= itemId %>" class="button">Back to Item</a>
</body>
</html>